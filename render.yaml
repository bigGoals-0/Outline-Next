services:
  - type: web
    name: outline-next-server
    runtime: docker
    plan: free
    region: singapore
    healthCheckPath: /actuator/health
    autoDeploy: true
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: OUTLINE_UPLOAD_DIR
        value: /var/outline/uploads
